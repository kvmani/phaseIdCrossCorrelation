# Configs

YAML configuration templates live here.

- `configs/ml/`: dataset prep, training, benchmark suite, and GUI-compatible ML configs

Start from the templates here, then use `docs/ml_classifier_workflow.md` and `docs/ml_input_data_runbook.md` for the matching contracts.
